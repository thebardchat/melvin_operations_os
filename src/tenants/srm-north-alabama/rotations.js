// Rotation state — epoch-based cycle day calculation
// Cycle starts on a known Monday (adjust EPOCH_DATE as needed)
export const ROTATION_EPOCH = '2024-01-01' // Monday baseline — adjust to real start date
export const BP_CYCLE_LENGTH = 3           // A/B/C groups rotate every 3 days
export const ROTA_EPOCH = '2024-01-01'     // Same baseline for per-crew rota cycles
